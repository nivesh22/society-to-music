# Local development dependencies — NOT installed in the Docker image.
# Install with: uv pip install -r requirements-dev.txt
-r requirements.txt

# Notebook
ipython==8.27.0
jupyterlab==4.2.5
notebook==7.2.2

# Pipeline visualisation
kedro-viz==9.2.0

# CI / linting / testing
pre-commit==3.8.0
ruff==0.6.9
pytest==8.3.3
pytest-cov==5.0.0
pytest-timeout==2.3.1
